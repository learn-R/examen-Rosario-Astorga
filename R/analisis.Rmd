---
title: "Un análisis a los factores determinantes en la percepción de la carga de trabajos de cuidado durante la pandemia en Chile"
subtitle: |
  | Examen: Proyecto final 
  | Universidad Alberto Hurtado
  | Análisis de Datos en R
  | Profesora: Valentina Andrade
  | Ayudantes: Dafne Vargas y Nicolás Godoy
author: "Rosario Astorga Pinto"
date: "22/11/2021"
output:
  html_document: 
    theme: cosmo
    toc_depth: 2
    toc_float: yes
    highlight: pygments
    code_folding: show
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = F, warning = F, message = F)
Sys.setlocale("LC_CTYPE", "spanish")
options(OutDec= ",", digits = 3)
```

# 1. Abstract (100-150 palabras)

::: {style="text-align: justify"}
La pandemia del COVID-19 ha demostrado que tanto las economías globales formales del mundo como nuestros hogares, se construyen sobre el trabajo doméstico y de cuidado, no reconocido ni remunerado que realizan mayoritariamente las mujeres y niñas, quienes en estos tiempos se han visto mayormente afectadas por la carga que significa la doble o triple jornada. La siguiente investigación se propone analizar los factores que influyen en la percepción de la variación de la carga de trabajos de cuidado en la pandemia en Chile, considerando el sexo, la edad, el trabajo productivo, los ingresos, las labores domésticas y el estado de salud mental. Para efectos de este trabajo, se utilizó la base de datos MOVID-IMPACT (2020) y mediante un análisis de regresión logística se concluye que……..Tal relación puede ser explicada por o También fue posible determinar que
:::

# 2. Introducción (200-250 palabras)

::: {style="text-align: justify"}
El trabajo de cuidados es un tema que históricamente se le ha atribuido a las mujeres....el asunto en cuanto a la corresponsabilidad social del cuidado como principio rector de la sociedad es una realidad que se hace urgente y que ha quedado en evidencia durante esta crisis sanitaria.....

El principio de corresponsabilidad social de los cuidados desde la desfeminización y colectivización de los cuidados como principio rector de la organización social 

Esta realidad, en el actual contexto de pandemia, persiste con una magnitud preocupante. Así lo han advertido organismos como ONU Mujeres y Cepal, que en el informe Cuidados en América Latina y el Caribe en tiempos de COVID-19: hacia sistemas integrales para fortalecer la respuesta y la recuperación (agosto 2020), señalan que la crisis “ha demostrado la insostenibilidad de la actual organización social de los cuidados, intensificando las desigualdades económicas y de género existentes, puesto que son las mujeres más pobres quienes más carga de cuidados soportan y a quienes la sobrecarga de cuidados condiciona, en mayor medida, sus oportunidades de conseguir sus medios para la subsistencia”.


Pregunta guía: ¿Qué factores influyen en la percepción de la variación de la carga de trabajos de cuidado en pandemia en Chile?

Objetivo general: analizar factores influyen en la percepción de la variación de la carga de trabajos de cuidado en la pandemia en Chile



:::

# 3. Análisis (300-500 palabras)

## 3.1 Carga de paquetes

```{r carga_paquetes, message=FALSE, warning=FALSE, include=FALSE}

pacman::p_load(dplyr, #Para manipular datos
               tidyverse, #Universo de paquetes
               magrittr, #Para utilizar el operador pipe
               srvyr, #Para trabajar con muestras complejas
               survey,#para estimar los modelos con peso muestral
               rmdformats, #Proporciona varios formatos de salida HTML de estilos únicos y atractivos
               remotes, #Para instalar texreg desde GitHub
               texreg, #Para crear tablas en formato publicable
               sjPlot) #Para visualizar los datos
    
  
```

## 3.2 Carga de datos

```{r carga_datos, message=FALSE, warning=FALSE, include=FALSE}
datos_proc <- readRDS("../output/datos/datos_proc.rds")
```

### 3.3 Creación objeto encuesta

# Creamos un nuevo objeto llamado movid_objeto con la información de movid_proc, luego aplicamos diseño muestral, especificando los ids a partir de 1 (entrevistado seleccionado) y el ponderador con factor de expansión. 

```{r creacion_objeto_encuesta, message=FALSE, warning=FALSE, include=FALSE}
movid_objeto <- datos_proc %>% #Creamos un nuevo objeto llamado movid_objeto con la información de datos_proc
  as_survey_design(ids = 1, #Aplicamos diseño muestral, especificando los ids a partir de 1 (entrevistado seleccionado),
                   weights = factor_expansion) #y el ponderador con factor de expansión 

```



## 3.4 Modelo de regresión logística binaria

### a. Construcción múltiple modelos logísticos

A continuación se crean diferentes modelos:

```{r modelos_logisticos, message=FALSE, warning=FALSE}

modelo0 <- glm(variacion_cuidados ~ 1, family = binomial(link = "logit"), data = datos_proc)
modelo3 <- glm(variacion_cuidados ~ sexo, family = binomial(link = "logit"), data = datos_proc)
modelo4 <- glm(variacion_cuidados ~ estado_sm, family = binomial(link = "logit"), data = datos_proc)
modelo5 <- glm(variacion_cuidados ~ sexo + estado_sm, family = binomial(link = "logit"), data = datos_proc)
modelo6 <- glm(variacion_cuidados ~ sexo + edad + trabajo_productivo + estado_sm + ingresos + variacion_td, family = binomial(link = "logit"), data = datos_proc)
modelo6survey <- svyglm(variacion_cuidados ~ sexo + edad + trabajo_productivo + estado_sm + ingresos + variacion_td, family = binomial(link = "logit"), design = movid_objeto, data = datos_proc)

```



### b. Representación modelos en una tabla

Luego, se representan los modelos en una tabla única con coeficientes exponenciados:

```{r tabla_modelos_logisticos}
sjPlot::tab_model(list(modelo0,modelo3,modelo4,modelo5,modelo6),
  string.intercept = "(Intercepto)",
  string.pred = "Predictores",
  string.est = "Estimación (OR)",
  p.style = "stars",
  collapse.ci = T,
  digits = 3,
  dv.labels = c("Modelo 1", "Modelo 3", "Modelo 4", "Modelo 5", "Modelo 6"),
  transform = "exp",
  show.reflvl = TRUE,
  encoding = "UTF-8",
  lang = "es")
```



### c. Interpretación 


::: {style="text-align: justify"}
Respecto a los coeficientes del modelo 6, es posible mencionar que para la variable **"edad"**, las chances de cumplir con las medidas de cuidado social para los **adultos**, controlando el efecto de las otras variables, son un **11,5% mayores** respecto a las **chances** de cumplir con las medidas de cuidado para los **jóvenes**. En cambio, los **adultos mayores** poseen **38,3% más chances** de cumplir con las medidas de cuidado social respecto a las chances de los jóvenes de cumplir con las medidas de cuidados contra el COVID-19.

Con respecto a la variable **"trabaja"**, las chances de que una persona que **no trabaja** cumpla con las medidas de cuidado ante el COVID-19 son **0,7% menores** a las chances de que una persona que **sí trabaja** cumpla con las medidas de cuidado.

En relación a la variable sobre **percepción de riesgo** al COVID-19, las chances de que una persona que percibe como **algo peligroso** al virus cumpla con las medidas de cuidado son **147,5% mayores** a las chances de que una persona que percibe como nada peligroso cumpla con las medidas de cuidado ante el virus. Por un lado, las chances de percibir al COVID-19 como **bastante peligroso** son **234,9% mayores** a las chances de percibirlo como nada peligroso. Por otro lado, las chances de cumplir con las medidas de cuidado de quienes perciben al virus como **muy peligroso** son **328,3% mayores** a las chances de cumplir con las medidas de cuidado de quienes perciben al virus como nada peligroso. Por último, las chances de que una persona que considera al virus como **extremadamente peligroso** cumpla con las medidas de cuidado son **399,5% mayores** a las chances de que una persona que considera al virus como nada peligroso cumpla con las medidas de cuidado.

Finalmente, para las variables **"edad"**, **"trabaja"**, las categorías "Nada peligroso", "Algo peligroso", "Bastante peligroso" de la variable "Riesgo", es posible mencionar que al **no** ser **significativas**, no existe un efecto a nivel muestral . Por lo tanto, cabe destacar que, por ejemplo, a medida que aumenta la edad no existe una diferencia en las chances de quienes cumplen y no cumplen con las medidas de cuidado social.
:::



### d. Gráfico de valores predichos con el modelo 6 para el trabajo de cuidados según sexo

***Gráfico modelo 6 de valores predichos trabajo de cuidados según sexo***

```{r valores_predichos_cuidados_edad, message=FALSE, warning=TRUE}
plot_model(modelo6, type = c("pred"),
           terms = c("sexo"),  digits = 3,
  title = "Gráfico 1. Modelo 6 logístico que estima la frecuencia de cuidarse según edad")
```



### e. Comparación y reporte estimación modelo 6 con R base y modelo 6 con diseño complejo de encuestas. 

***Reporte con summary de modelo 6 con R base y diseño complejo***

Primero se reporta el modelo 6 sin pesos muestrales:

```{r reporte_modelo6_R_base, message=FALSE, warning=FALSE}

summary(modelo6)

```

Luego se compara con el modelo con pesos muestrales:

```{r reporte_modelo6_diseño_complejo, message=FALSE, warning=FALSE}

summary(modelo6survey)

```

***Reporte con tab_model de modelo 6 con R base y diseño complejo***

Asimismo, se reporta la tabla del modelo 6 con R base:

```{r tabla_modelo6_R_base, message=FALSE, warning=FALSE}
sjPlot::tab_model(modelo6, 
                  show.ci=FALSE, 
                  transform = 'exp', 
                  p.style = "stars", 
                  df.method = 'wald', 
                  dv.labels = "Modelo 6 con R base", 
                  string.pred = "Predictores", string.est = "β", 
                  encoding =  "UTF-8")
```

Para luego compararla con la tabla del modelo 6 con diseño complejo:

```{r tabla_modelo6_diseño_complejo, message=FALSE, warning=FALSE}
sjPlot::tab_model(modelo6survey, 
                  show.ci=FALSE, 
                  transform = 'exp', 
                  p.style = "stars", 
                  df.method = 'wald',
                  dv.labels = "Modelo 6 diseño complejo", 
                  string.pred = "Predictores", string.est = "β", 
                  encoding =  "UTF-8")
```


### f. Ajuste del modelo




# 4. Conclusión (150-200 palabras) 






# 5. Referencias 



